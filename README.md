# For run the application
```bash
./venv/bin/uvicorn main:app --reload
```

# For create the virtual environment (Only first time)
```bash
python3 -m venv venv
```

# For install the dependencies
```bash
./venv/bin/pip install -r requirements.txt
```
